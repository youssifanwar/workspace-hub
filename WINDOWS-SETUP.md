# 🏢 WorkSpace Hub — دليل التنصيب على ويندوز

هذا الدليل يشرح خطوة بخطوة إزاي تحول المشروع لبرنامج ويندوز مستقل يتسطب بملف `.exe`.

---

## 📋 المتطلبات

### مطلوب على جهازك (مرة واحدة فقط)

1. **Node.js 20 أو أحدث**  
   حمّله من: <https://nodejs.org>

2. **PostgreSQL 16 أو أحدث**  
   حمّله من: <https://www.postgresql.org/download/windows/>  
   خلال التنصيب:
   - كلمة السر للـ `postgres` user: خليها `postgres` (أو أي حاجة تفتكرها)
   - البورت: `5432`
   - اترك باقي الإعدادات افتراضية

3. **Git** (اختياري)  
   <https://git-scm.com/download/win>

---

## 🚀 خطوات بناء ملف التنصيب

### 1. جهّز قاعدة البيانات

افتح **pgAdmin** أو **psql** واعمل قاعدة بيانات جديدة:

```sql
CREATE DATABASE workspace_hub;
```

### 2. عدّل ملف `.env`

في مجلد المشروع، افتح ملف `.env` وضيف كلمة السر بتاعتك:

```
DATABASE_URL=postgresql://postgres:كلمة_السر@127.0.0.1:5432/workspace_hub
```

### 3. نصّب اعتماديات المشروع

افتح **PowerShell** أو **Command Prompt** في مجلد المشروع واكتب:

```bash
npm install
```

### 4. طبّق مخطط قاعدة البيانات

```bash
npx drizzle-kit push
```

### 5. ابنِ ملف التنصيب

```bash
npm run dist:win
```

### 6. جاهز! 🎉

بتلاقي ملف التنصيب في مجلد `dist/`:
```
dist/WorkSpaceHub-Setup-1.0.0.exe
```

ده الملف اللي بتوزعه على أي جهاز ويندوز. لما اليوزر يشغّله بيتسطب زي أي برنامج عادي:

- شورت كت على سطح المكتب
- شورت كت في قائمة ابدأ
- بيظهر في "إضافة أو إزالة البرامج"

---

## 💻 تشغيل البرنامج في وضع التطوير

لو عايز تشغل البرنامج مباشرة من غير ما تعمل installer (مفيد وقت التطوير):

```bash
# Terminal 1 — يبني ويشغل standalone server
npm run electron:build

# Terminal 2 — يفتح نافذة البرنامج
npm run electron:dev
```

أو بشكل أبسط لأول مرة:
```bash
npm run electron:build
npm run electron:dev
```

---

## 🔧 على أجهزة الموظفين (بعد التنصيب)

### الطريقة 1 — قاعدة بيانات على نفس الجهاز

كل جهاز عليه PostgreSQL وقاعدة بياناته الخاصة. البرنامج بيقرأ الإعدادات من ملف `.env` الموجود في مجلد التنصيب أو في:

```
%APPDATA%\WorkSpace Hub\.env
```

### الطريقة 2 — قاعدة بيانات مشتركة على السيرفر (الأفضل)

على جهاز واحد في المكان يبقى فيه PostgreSQL، وكل الأجهزة الباقية تتصل به.

على جهاز السيرفر:
1. نصّب PostgreSQL
2. في ملف `postgresql.conf` غيّر:
   ```
   listen_addresses = '*'
   ```
3. في ملف `pg_hba.conf` أضف:
   ```
   host  all  all  192.168.1.0/24  md5
   ```
4. افتح البورت 5432 في Windows Firewall
5. اعمل Restart للـ PostgreSQL Service

على جهاز الموظف، بعد ما البرنامج يتسطب:
1. افتح `%APPDATA%\WorkSpace Hub\`
2. أنشئ ملف اسمه `.env` وحط فيه:
   ```
   DATABASE_URL=postgresql://postgres:كلمة_السر@192.168.1.100:5432/workspace_hub
   ```
   (غيّر `192.168.1.100` لـ IP جهاز السيرفر)

كل الأجهزة كده هتشتغل على نفس قاعدة البيانات → نفس البوكينج ونفس الشيفتات لحظياً.

---

## 🔐 الحسابات الافتراضية

أول ما تشغل البرنامج بيتعمل تلقائي:

| اليوزر | الباسورد | الصلاحية |
|--------|-----------|----------|
| `admin` | `admin123` | مدير عام (كل الصلاحيات) |
| `manager` | `manager123` | مدير فرع |
| `employee` | `employee123` | موظف كاشير |

**⚠️ مهم جداً:** غيّر كلمات السر دي فوراً من صفحة **Settings** بعد أول دخول.

---

## 🖨️ إعداد طابعة الفواتير الحرارية

الفواتير مصممة على حجم إيصال حراري (~80mm).

1. وصّل الطابعة الحرارية بالجهاز
2. من الويندوز → Devices and Printers → اضبطها كافتراضية
3. من داخل البرنامج، لما تعمل Checkout بيفتح صفحة الفاتورة، اضغط زر **Print** (أو Ctrl+P)
4. في نافذة الطباعة، اختار الطابعة الحرارية

**لو الفاتورة طويلة زيادة:** روح Chrome/Edge print settings → Paper size → مخصص → 80mm

---

## 📱 نظام QR Ordering (طلب من الموبايل)

كل مكتب/غرفة اجتماعات ليها QR Code خاص بها.
لما العميل يمسحه بموبايله بتفتح له صفحة منيو موبايل بيختار منها ويضيف طلبه على فاتورته
تلقائياً. الطلب بيوصل للكمبيوتر بصوت وإشعار، وبيتطبع تلقائياً على طابعة المطبخ.

### خطوات التفعيل

**1. Open Windows Firewall port 39217** (البورت الداخلي للبرنامج)

من PowerShell كـ Administrator:
```powershell
New-NetFirewallRule -DisplayName "WorkSpace Hub" -Direction Inbound -Protocol TCP -LocalPort 39217 -Action Allow
```

**2. تأكد إن الموبايل والكمبيوتر على نفس الشبكة (Wi-Fi)**

**3. من البرنامج → Settings → Public URL for QR menu**

هيظهرلك الـ LAN IP اللي البرنامج اكتشفه (مثلاً `192.168.1.20`).
- لو صحيح، سيبه فاضي وهيتستخدم تلقائياً.
- لو استخدمت hostname أو IP ثابت، حطه في الخانة (زي `http://192.168.1.20:39217`).

**4. من البرنامج → Settings → Printing & QR ordering**
- فعّل "Auto-print new QR orders"
- اختار "Kitchen printer" (طابعة تذاكر الطلبات)
- اختار "Invoice printer" (طابعة الفواتير)
- اضغط 🧪 Test print للتأكد

**5. من البرنامج → QR Codes**
اختار المكاتب اللي عايز تطبع QR ليها → 🖨 Print selected → يطلع صفحة A4 فيها كارتين QR
منسّقين، الصقهم على المكاتب أو غرف الاجتماعات.

### دورة العميل
1. العميل يوصل → الموظف يعمل Check-in من صفحة Bookings
2. العميل يمسح QR اللي على المكتب → يفتح المنيو
3. يختار طلبه → Place Order
4. الطلب يوصل للكمبيوتر بصوت 🔔، ويتطبع تلقائياً على طابعة المطبخ
5. الباريستا يشوف التذكرة المطبوعة (مفيش شاشة مطلوبة عنده)
6. لما العميل يكمل، الموظف يعمل Checkout عادي — الطلبات كلها متجمعة في فاتورته

### شاشة Live Orders (اختيارية)
لو الموظف عايز يشوف الطلبات على الشاشة بدل الطباعة، من الـ Sidebar فيه صفحة
"Live Orders" بتعرض كل التذاكر لايف، مع أزرار Reprint و Served.

---

## 🆘 مشاكل شائعة

### "DATABASE_URL is not set"
تأكد إن ملف `.env` موجود وفيه سطر `DATABASE_URL=...` صحيح.

### "Could not start WorkSpace Hub"
- تأكد إن PostgreSQL شغّال (services.msc → PostgreSQL)
- تأكد إن قاعدة البيانات `workspace_hub` موجودة
- شغّل `npx drizzle-kit push` مرة تانية لو أول تشغيل

### البرنامج بطيء في أول تشغيل
طبيعي — بيبني قاعدة البيانات وبيسيد البيانات الأولية (الحسابات + المكاتب + غرف الاجتماعات + منيو تجريبي).

### عايز أعمل نسخة احتياطية Backup
```bash
pg_dump -U postgres workspace_hub > backup.sql
```

استعادتها:
```bash
psql -U postgres workspace_hub < backup.sql
```

---

## 📦 حجم ملف التنصيب

- Installer: ~85 MB
- بعد التنصيب: ~200 MB
- استهلاك RAM: ~250 MB

---

## 🎨 لتخصيص الأيقونة

استبدل ملف `electron/icon.png` بأي صورة PNG بحجم 1024×1024 بكسل، ثم أعد بناء ملف التنصيب.

---

**تم بنجاح** — لو محتاج مساعدة، الملف مصمم يشتغل out-of-the-box على ويندوز 10/11 (64-bit).
